html
head
    title= "Error"
    link(rel="stylesheet", type="text/css", href="/css/error.css")
    script(src="/jquery-3.5.0.js")
body
    h1= "Uh-oh, there seems to be a problem..."
    h2(data-testid="errorMsg")= message
    img(src="/nooo.jpg")
    button(id="gobackBtn", class="btn") Go back
    script.
        "use strict";
        $("#gobackBtn").on("click", (event) => {
            window.location.replace("/");
        });
